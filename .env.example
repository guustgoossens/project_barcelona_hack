# Frontend (Vite/TanStack Start)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex runtime env (set via `bunx convex env set ...`)
PYTHON_INFERENCE_URL=https://your-northflank-url
INFERENCE_TOKEN=shared-bearer-token

# Python backend
HF_TOKEN=hf_xxx                # required for gated LLaMA 3.2-3B
TRIBE_CACHE=./cache
